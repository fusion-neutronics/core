[package]
name = "yamc-rng"
version = "0.1.0"
edition = "2021"
description = "Shared PCG random-number stream for yamc -- per-history and per-secondary seeding, mirrored by the GPU kernel"
license = "MIT"

[lib]
name = "yamc_rng"
path = "src/lib.rs"

[dependencies]
